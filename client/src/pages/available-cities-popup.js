import React, {useEffect} from 'react';
import { Header } from '../components/js/header';
import {Footer} from '../components/js/footer'
import {AvailableCities} from '../components/js/avaliable-city-popup'
import Aos from 'aos';

const Cities =  () =>{

        return(
            <AvailableCities/>
        )
}
export {Cities}